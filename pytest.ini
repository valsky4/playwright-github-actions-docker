[pytest]
addopts = --tracing=retain-on-failure --headed
#--headed --device="iPhone 12" --browser webkit
#addopts = --tracing on --screenshot on
#addopts = --headed

markers =
    api: mark a test as an api test
    web: mark a test as an web test